'use client'; import Link from 'next/link'; import {useEffect,useState} from 'react'; import {siteConfig} from '@/config/site';
export function Header(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);const links=[['O Escritório','#escritorio'],['Áreas de Atuação','#areas'],['Profissionais','#profissionais'],['Conteúdos','#conteudos'],['Contato','#contato']];useEffect(()=>{const handleScroll=()=>setScrolled(window.scrollY>8);handleScroll();window.addEventListener('scroll',handleScroll,{passive:true});return()=>window.removeEventListener('scroll',handleScroll)},[]);return <header className={`header${scrolled?' is-scrolled':''}`}><Link href="#inicio" className="brand"><b>{siteConfig.monogram}</b><span>{siteConfig.shortName}<small>ADVOGADOS ASSOCIADOS</small></span></Link><nav className="nav">{links.map(x=><Link key={x[1]} href={x[1]}>{x[0]}</Link>)}</nav><Link className="head-cta" href="#contato">ENTRAR EM CONTATO</Link><button className="menu" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-navigation" aria-label={open?'Fechar menu':'Abrir menu'}>{open?'Fechar':'Menu'}</button>{open&&<nav className="mobile-nav" id="mobile-navigation">{links.map(x=><Link key={x[1]} href={x[1]} onClick={()=>setOpen(false)}>{x[0]}</Link>)}</nav>}</header>}
